# Core packages with Python 3.12 compatibility
numpy>=1.26.0
pandas>=2.0.0
matplotlib>=3.7.1
seaborn>=0.13.0

# Streamlit
streamlit>=1.32.0

# Machine learning (use lightweight alternatives)
scikit-learn>=1.3.0

# For collaborative filtering (lightweight alternatives)
# Note: scikit-surprise is removed because it's incompatible with Python 3.12

# For NLP (simplified)
gensim>=4.3.1

# Optional
sentence-transformers>=2.2.2